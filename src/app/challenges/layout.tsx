export const metadata = {
  title: 'Challenges | CourtMate',
  description: 'Post or accept 1v1 and team challenges on campus',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
