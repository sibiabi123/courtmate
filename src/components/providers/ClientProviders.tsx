'use client';

import React from 'react';

// Lenis smooth scroll removed — not needed for CourtMate's clean design

export function ClientProviders({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
